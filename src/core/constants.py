"""
Global application constants.
"""

from src.version import APP_NAME, APP_VERSION

LOG_FILENAME = "localization_studio.log"

DEFAULT_WINDOW_WIDTH = 1200
DEFAULT_WINDOW_HEIGHT = 800

JSON_FILE_FILTER = (
    "JSON files (*.json);;"
    "All files (*.*)"
)